//undefined means a variable has been declared but not assigned a value, 
//while null means a variable is intentionally assigned an empty value by the developer.

let a;
console.log(a); // undefined (not assigned)

let b = null;
console.log(b); // null (intentionally empty)